# MortalSim 使用指南

1. 打开 `MortalSim.exe`。
2. 在“新建分析”输入手牌、第一摸、宝牌和候选第一打。
3. 设置局数和 seed；严格比较时保持候选使用相同 seed。
4. 点击“开始模拟”，运行页会显示批次进度和 GPU 状态。
5. 在结果页查看平均得点、顺位、和了率、放铳率与候选差值。
6. 使用结果页的 JSON、CSV 或 HTML 链接导出当前结果。
7. 在历史运行中重新打开、复制或删除过去的配置。
