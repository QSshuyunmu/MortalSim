# MortalSim 使用指南

1. 打开 `MortalSim.exe`。
2. 在“新建分析”输入 14 张手牌、宝牌和候选第一打。手牌最后一张固定视为第一摸。
3. 设置局数和 seed；严格比较时保持候选使用相同 seed。
4. 点击“开始模拟”，运行页会显示批次进度和 GPU 状态。
5. 在结果页查看 NAGA 口径平均局收支、顺位、和了率、放铳率与候选差值。
6. 使用结果页的 Excel、JSON 或 HTML 导出：Excel 仅包含当前总表的全部统计指标，JSON 包含完整结果协议，HTML 可离线阅读。
7. 在历史运行中重新打开、复制或删除过去的配置。
